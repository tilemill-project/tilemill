/**
 * TileMill client configuration
 * ============================================================================
 * The following file is an example of settings you can specify for your
 * particular TileMill client. Uncomment & adjust any of the following lines
 * to suit your needs.
 */

/**
 * Example: Use the TileMill PHP backend available at http://tilemill.
 */
// TileMill.settings.server = 'simple';
TileMill.settings.simpleServer = 'http://localhost:8889/';

/**
 * Example: Use a local TileLive rasterizer at http://localhost:8888/
 */
// TileMill.settings.rasterizer = 'tilelive';
// TileMill.settings.tileliveServer = 'http://localhost:8888/';

/**
 * Example: Use the default HTML client runtime.
 */
// TileMill.settings.runtime = 'html';
