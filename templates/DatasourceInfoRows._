<% _(features).each(function (feature) { %>
<tr>
  <% _(feature).each(function (value, key) {  if (fields[key]) { %>
  <td class='<%=fields[key].type%>'><%= value %></th>
  <% }}); %>
</tr>
<% }); %>
